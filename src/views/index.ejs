<%- include('partials/header') %>

<main class="home">
  <h1>Welcome</h1>
  <% if (locals.user) { %>
    <p>Hello, <%= user.username %>! Ready to check your files?</p>
    <a href="/dashboard"><button type="button">Go to Dashboard</button></a>
  <% } else { %>
    <p><a href="/login">Log in</a> to get started.</p>
  <% } %>
</main>

<%- include('partials/footer') %>
