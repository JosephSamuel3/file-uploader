<%- include('partials/header') %>

<main>
  <h1>Welcome</h1>
  <% if (locals.user) { %>
    <p>Hello, <%= user.username %>!</p>
  <% } else { %>
    <p><a href="/login">Log in</a> to get started.</p>
  <% } %>
  <h1>check your files <a href="/dashboard">dashboard</a></h1>
</main>

<%- include('partials/footer') %>
